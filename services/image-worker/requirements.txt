# image-worker dependencies.
# NOTE: install a CUDA-matched torch build first, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then: pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
torch>=2.1
diffusers>=0.31
transformers>=4.44
accelerate>=0.30
safetensors>=0.4
Pillow>=10.0
sentencepiece>=0.2
protobuf>=4.25
